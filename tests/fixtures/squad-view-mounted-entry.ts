import React, { act, createElement } from "react";
import { createRoot } from "react-dom/client";

import { SquadView } from "../../app/teams/[slug]/squad/squad-view";

export { act, createElement, createRoot, React, SquadView };
